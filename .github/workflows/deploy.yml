name: Deployment

on:
  push:
    branches: [ master ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - uses: actions/setup-node@v2
      with:
        node-version: 12.x
    
    - name: Sync
      env:
        dest: 'root@34.220.189.164:/var/www/html/wordpress/wp-content/themes/zeever'
      run: |
        sudo echo "${{secrets.SERVER_SSH_KEY}}" > deploy_key
        sudo chmod 600 ./deploy_key
        sudo rsync -chav --delete \
          -e 'ssh -i ./deploy_key -o StrictHostKeyChecking=no' \
          --exclude /deploy_key \
          --exclude /.git/ \
          --exclude /.github/ \
          --exclude /node_modules/ \
          ./ ${{env.dest}}
